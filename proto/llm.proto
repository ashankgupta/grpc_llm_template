syntax = "proto3";

package llm;

service LLMService {
  rpc Generate (Prompt) returns (stream Token);
}

message Prompt {
  string text = 1;
  float temperature = 2;
  float top_p = 3;
  int32 top_k = 4;
}

message Token {
  string text = 1;
}
